import React from "react";
import type { NextPage } from "next";
import Bar from "../components/Bar";

const Home: NextPage = () => {
  return (
    <div>
      <Bar />
    </div>
  );
};

export default Home;
